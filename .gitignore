# Credentials — never commit the real secrets file
src/secrets.py

# Personal photos (slideshow images — copy to SD card manually)
photos/

# Personal photos in Utils — originals, processed output, samples
Utils/originals archive/
Utils/transfered/
Utils/presto_photos/

# Custom arrival screen images (personal)
src/arrived.jpg
src/arrived2.jpg

# Claude Code session data
.claude/

# MicroPython compiled bytecode
*.mpy
__pycache__/
