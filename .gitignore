# Credentials — never commit the real secrets file
src/secrets.py

# Personal photos (slideshow images — copy to SD card manually)
photos/

# Personal photos in Utils — originals and processed output
Utils/originals archive/
Utils/transfered/

# Personal arrival screen variant
src/arrived2.jpg

# Claude Code session data
.claude/

# MicroPython compiled bytecode
*.mpy
__pycache__/
